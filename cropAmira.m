%% Function to crop amira datasets, to just simulate on a small portion of
%the network.
%
% Inputs:
%       + full  -  Amira dataset to be cropped.
%       + X     -  Two element vector of X range.
%       + Y     -  Two element vector of Y range.
%       + Z     -  Two element vector of Z range.
%
% Outputs:
%       + cropped  -  Cropped amira dataset

function [cropped] = cropAmira(full, X, Y, Z)



end